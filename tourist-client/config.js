// Set apiBaseUrl to the deployed backend origin (without a trailing slash) for GitHub Pages.
// Leave it blank to retain same-origin local development behavior.
window.APP_CONFIG = {
    apiBaseUrl: "",
};
